# T05 — Evidência de navegador e prova de dado inalterado

Task Contract derivado do plano `PLAN_VALID` de F-026. Auto-contido.

## Identity

```text
feature_id: F-026
task_id: T05
parent_plan: docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/plan.md
depends_on: T01, T04
```

## Goal

Existe evidência de navegador, presa à revisão aprovada do DAP, mostrando o shell tokenizado em
desktop e mobile, e a prova de que nenhum campo consumido mudou. Verificável: as capturas existem
sob `docs/features/F-026-.../evidence/browser/` e `npm run test:contract` está verde sem mudança de
fixture.

## Scope

- Capturas de navegador (desktop e mobile 390×844) das superfícies tocadas — sidebar, topbar,
  status-card, métricas, jornada, pendências, abas longas —, comparadas às `captures-r4/` da F-025.
- Salvar as capturas em `docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/evidence/browser/`.
- Rodar `npm run test:contract`.

## Out of Scope

- Qualquer mudança de estilo ou de código de aplicação (isso foi T01–T04).
- Alterar fixtures de teste.

## Acceptance Criteria

- Capturas desktop + mobile 390×844 presentes sob `evidence/browser/`, correspondendo à revisão
  aprovada do DAP. Como se checa: listar o diretório e conferir contra as superfícies da §06–§09 do
  DAP.
- `npm run test:contract` verde, **sem** mudança de `tests/fixtures/dashboard.mjs`. Como se checa:
  `npm run test:contract` e `git status tests/fixtures/`.
- Foco/teclado validados: o anel de foco da F-025 (2 px na cor da marca, 2 px de afastamento) segue
  visível; nenhum `outline: none` cru introduzido. Como se checa: navegar por Tab no shell.

## Validation

```text
baseline: npm run test:contract → verde em main; stack sobe com docker compose up -d
required: web-unit-contract: npm run test:contract
          browser-runtime-validation: npm run build && npm start  (capturas na máquina)
```

`npm run test:e2e` (Playwright) e as capturas rodam **na máquina** com a stack de pé
(`docker compose up -d`); o perfil de navegador não roda no CI (ver `docs/project-context.md`).

## Required Capabilities

```text
READ:     app/, components/, docs/features/F-026-.../
WRITE:    docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/evidence/browser/
VALIDATE: web-unit-contract (npm run test:contract); browser-runtime-validation (na máquina)
COMMIT:   allowed  (merge é gate humano)
```

## Context to Read First

- `docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/design-approval.md` — §06–§09 (as
  superfícies e estados a capturar) e as `captures-r4/` da F-025 como referência de aparência.
- `docs/project-context.md` — perfil `browser-runtime-validation` roda na máquina, não no CI.
- `CLAUDE.md` — comandos web e como subir a stack.

## Known Risks

- Capturar numa fonte diferente da do produto (a F-025 mediu esse defeito: Inter vs system-ui).
  Capture com a stack real de pé, onde o `next/font/google` auto-hospeda a Inter.
- Um wrong turn: mudar estilo "para a captura ficar melhor" — esta tarefa não altera estilo.

## Human Gates

- Merge do branch da tarefa é gate humano. `DONE` da feature exige evidência de navegador
  (desktop + mobile), revisão e decisão humana.

## Reporting

Encerre com o `BUILD REPORT` completo (todos os campos; `none` onde vazio):

```text
BUILD REPORT
Status: BUILD_COMPLETE | BUILD_BLOCKED | BUILDER_VALIDATION_BLOCKED | BUILDER_CONTRACT_INCOMPLETE
Files changed:
Validation executed:
Validation skipped:
Unavailable capabilities:
Review repair trigger: none
Review feedback iteration: 0
CI repair trigger: none
CI repair iteration: 0
Assumptions:
Remaining risks:
Human decisions required:
```
