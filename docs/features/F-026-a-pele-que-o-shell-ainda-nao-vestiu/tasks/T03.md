# T03 — Mapa §2: status-card, métricas, jornada, pendências, e as abas longas

Task Contract derivado do plano `PLAN_VALID` de F-026. Auto-contido.

## Identity

```text
feature_id: F-026
task_id: T03
parent_plan: docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/plan.md
depends_on: T02
```

## Goal

Os seletores de §2 e os das **abas longas** em `app/globals.css` passam a citar tokens semânticos,
conforme o mapa do DAP. As abas longas (Resultados/Documentos/Cronograma/Reuniões/Decisões) entram
**nesta fatia** (decisão do gate). Verificável: nenhum `slate-*`/`bg-white`/`amber-*`/`rose-*` cru
remanescente nos seletores de §2 nem nos das abas longas.

## Scope

- `app/globals.css`, seletores de §2: `.status-card`, `.progress`, `.status-meta`, `.timeline-dot`,
  `.pending-avatar`, `.priority-pill`, `.file-icon`, `.comment-input`, `.filter-chip`.
- `app/globals.css`, seletores das abas longas — aplicar o **mesmo mapa** §1–§2 (herança, sem token
  novo).
- Mapa: `bg-white`→`bg-surface`; `bg-slate-200/70`→`bg-line`; `text-slate-400`→`text-muted`
  (contraste); `border-slate-300`→`border-line`; `bg-amber-100/text-amber-800`→
  `bg-warning-50/text-warning-600`; `bg-slate-100`→`bg-surface-sunken`.

## Out of Scope

- Sidebar/topbar (é T02).
- Substituir estados/botões pelas primitivas (é T04).
- Tokens de raio (não aprovado nesta fatia).
- Qualquer valor novo fora do `@theme` existente.

## Acceptance Criteria

- Nenhum `slate-*`/`bg-white`/`amber-*`/`rose-*` cru nos seletores de §2 e das abas longas. Como se
  checa: inspecionar essas regras em `app/globals.css`.
- Correção de contraste aplicada (`slate-400`→`muted` onde o DAP a marca não-isovalente).
- Guarda de consumo de token verde: `npm test`.
- Dado inalterado: `npm run test:contract` verde.

## Validation

```text
baseline: npm test  → verde (com T02 já integrada) antes da mudança
required: web-unit-contract: npm test
          web-unit-contract: npm run test:contract
```

## Required Capabilities

```text
READ:     app/globals.css, docs/features/F-026-.../design-approval.md, docs/design/one-design-system.md
WRITE:    app/globals.css  (só os seletores de §2 e das abas longas)
VALIDATE: web-unit-contract (npm test; npm run test:contract)
COMMIT:   allowed  (merge é gate humano)
```

## Context to Read First

- `docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/design-approval.md` — tabela §2 e a
  decisão "abas longas = nesta fatia" (Open questions resolvidas).
- `docs/design/one-design-system.md` — tokens e medições de contraste.
- `app/globals.css` — regras de §2 e das abas.

## Known Risks

- As abas longas usam seletores que podem repetir padrões de §1/§2; aplicar o mapa consistente, sem
  inventar valor. Um wrong turn: introduzir um token de cor novo — não há novo nesta fatia.
- PARALLELISM_RISK: `app/globals.css` compartilhado; integrar após T02, antes de T04.

## Human Gates

- Escopo das abas (nesta fatia) já é decisão do gate. Merge do branch é gate humano.

## Reporting

Encerre com o `BUILD REPORT` completo (todos os campos; `none` onde vazio):

```text
BUILD REPORT
Status: BUILD_COMPLETE | BUILD_BLOCKED | BUILDER_VALIDATION_BLOCKED | BUILDER_CONTRACT_INCOMPLETE
Files changed:
Validation executed:
Validation skipped:
Unavailable capabilities:
Review repair trigger: none
Review feedback iteration: 0
CI repair trigger: none
CI repair iteration: 0
Assumptions:
Remaining risks:
Human decisions required:
```
