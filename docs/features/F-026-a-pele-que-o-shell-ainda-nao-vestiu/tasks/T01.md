# T01 — Naming provisório remanescente → One

Task Contract derivado do plano `PLAN_VALID` de F-026. Um Builder não cria a própria tarefa.
Este contrato é auto-contido: assuma que o executor tem o Core, este contrato e o repositório.

## Identity

```text
feature_id: F-026
task_id: T01
parent_plan: docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/plan.md
depends_on: none
```

## Goal

A única ocorrência de "portal" como nome de produto na superfície do cliente vira "One". Verificável:
`app/DashboardClient.tsx` deixa de conter a string "Notificações no portal" e passa a conter
"Notificações no One"; nenhuma outra string de marca "Portal"/"Portal Labs" aparece em `app/`.

## Scope

- `app/DashboardClient.tsx` — trocar o texto "Notificações no portal" por "Notificações no One".

## Out of Scope

- Qualquer remapeamento de token/cor (é T02/T03).
- Qualquer adoção de primitiva (é T04).
- A palavra "portal" em comentários de código ou em superfícies de `/admin/*` — deixe como está.
- Textos de marca em `docs/` (histórico) — não tocar.

## Acceptance Criteria

- `app/DashboardClient.tsx` contém "Notificações no One" e não contém "Notificações no portal".
  Como se checa: `grep -n "Notificações no" app/DashboardClient.tsx`.
- Nenhuma string "Portal Labs"/"Portal do Cliente" em `app/`. Como se checa:
  `grep -rniE "portal ?labs|portal do cliente" app/` retorna vazio (exit 1).
- Web verde. Como se checa: `npm test` conclui sem falha (faz `npm run build` e roda os
  `node --test`, incluindo `tests/rendered-html.test.mjs`).

## Validation

```text
baseline: npm test  → verde em main antes da mudança
required: web-unit-contract: npm test
```

`npm test` encadeia `npm run build && node --test` (a SSR sobe `next start`). Para rodar só o teste
de SSR após um build: `node --test tests/rendered-html.test.mjs`.

## Required Capabilities

```text
READ:     app/, components/, docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/
WRITE:    app/DashboardClient.tsx
VALIDATE: web-unit-contract (npm test)
COMMIT:   allowed  (commita o branch da tarefa; merge é gate humano)
```

## Context to Read First

- `docs/features/F-026-a-pele-que-o-shell-ainda-nao-vestiu/design-approval.md` — a cópia
  "Notificações no One" foi **aprovada como cópia** no gate (26/08/2026).
- `app/DashboardClient.tsx` (a tela de configurações onde a string vive).

## Known Risks

- Trocar uma ocorrência errada de "portal" (ex.: um comentário). Só a string visível
  "Notificações no portal" muda. Um wrong turn: alterar `/admin/*` ou comentários.

## Human Gates

- Merge do branch da tarefa é gate humano.

## Reporting

Encerre com o `BUILD REPORT` completo (todos os campos; `none` onde vazio):

```text
BUILD REPORT
Status: BUILD_COMPLETE | BUILD_BLOCKED | BUILDER_VALIDATION_BLOCKED | BUILDER_CONTRACT_INCOMPLETE
Files changed:
Validation executed:
Validation skipped:
Unavailable capabilities:
Review repair trigger: none
Review feedback iteration: 0
CI repair trigger: none
CI repair iteration: 0
Assumptions:
Remaining risks:
Human decisions required:
```
