# ADR 0003 — Identidade

**Status:** Aceito

Keycloak fornece OIDC para ambiente local. O navegador mantém sessão protegida e a API valida token e associações de projeto. O modo de demonstração só é permitido com `DEMO_MODE=true` no ambiente local.
